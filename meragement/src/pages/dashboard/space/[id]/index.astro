---
// src/pages/dashboard/space/[id]/index.astro
export const prerender = false;

import DashboardLayout from "@/layouts/DashboardLayout.astro";
import DashboardPage from "@/components/space/dashboard/DashboardPage";

const { id } = Astro.params;
---
<!-- <DashboardLayout title={`Dashboard ${id ? `- Space ${id}` : ""}`}> -->
<DashboardLayout title={`Dashboard ${id ? `- Space ${id}` : ""}`}>
    <DashboardPage projectId={id} client:load/>
</DashboardLayout>