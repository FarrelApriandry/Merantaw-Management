---
export const prerender = false;

import DiscussionsPage from "@/components/space/discussions/DiscussionsPage.jsx";
import DashboardLayout from "@/layouts/DashboardLayout.astro";

const { id } = Astro.params;
---
<DashboardLayout title={`Discussion ${id ? `- Space ${id}` : ""}`}>
    <DiscussionsPage projectId={id} client:load />
</DashboardLayout>
